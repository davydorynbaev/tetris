/*
 * This is your configuration file.  You can edit the values specified here to
 * your liking.  Once you're done editing, save the file and recompile the
 * program for the changes to be effective.
 */

/* Key bindings. */
#define KEY_MOVE_LEFT	('a')
#define KEY_MOVE_RIGHT	('d')
#define KEY_MOVE_DOWN	('s')
#define KEY_ROTATE	('w')

/* Initial delay between drops, in seconds. */
#define INITIAL_DELAY_SECONDS (0.8f)
